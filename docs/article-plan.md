# 后续技术拆解文章：用户要求与素材计划

用户于 2026-09-10 要求：先完成 1:1 效果复刻、Git 推送、Vercel 部署；文章后续再写。

作品页面只展示效果和简单可调参数，不放标题、署名、源码入口、解释或技术状态。技术说明、来源和验证都留在仓库；verify.html 仅供本地开发验证，不纳入线上构建。

文章必须包含：

- 明确标注原算法作者 Xor（@XorDev），作品 Cyclone [369]，原作 https://www.shadertoy.com/view/N3dGRM 。区分原作与本项目的 WebGPU 移植，署名放在文章和仓库中，作品画面不显示这些信息。
- GitHub 仓库地址 https://github.com/Hassabis/cyclone 、最终 Vercel 线上预览地址，以及文章对应的 Git 提交版本，方便读者运行和复现。
- 真实运行效果的 GIF，不用静态图伪装动画；配原作 / 移植并排对照、关键公式示意图和阶段渲染图。
- 原始 369 字符 GLSL 与可读版本逐段对应：像素射线、90 次步进、空间扭转、7 层正弦扰动、距离步长、发光积累和 tanh 色调映射。
- 我们的实现细节与注意事项。主实现是 vgpu / WebGPU / WGSL，WebGL2 运行原始 GLSL 作为验证基准；不要混称为同一种 API。
- GLSL 与 WGSL：坐标原点、像素中心、row-vector 矩阵乘法、列主序、向量 swizzle 写入、后置自增和 for 的执行顺序、未初始化局部变量、浮点误差、输出 alpha、sRGB 与线性目标格式。
- 性能优化：先实测，记录设备、浏览器、分辨率、DPR、帧率 / GPU 耗时、代码版本；区分不改变公式的工程优化与会改变画面的视觉近似。
- 逐像素验证的覆盖、阈值和局限，不能把容差内通过称为所有设备 bit-exact。
- 不能把 min(length(p.xz),8-abs(p.y)) 简化称作严格 SDF，也不能把它的发光累积称作完整物理体积散射。

建议图版：1. 首屏动态效果；2. 射线与坐标空间；3. 旋转前后；4. 正弦扰动 1–7 层；5. 单像素步进轨迹；6. 发光累加与 tanh 前后；7. GLSL/WGSL 差分；8. 性能曲线。

素材必须从固定时间、固定分辨率、记录版本的真实渲染生成；保留导出脚本、帧率与编码设置。当前阶段不发布文章、不上传公众号草稿。
